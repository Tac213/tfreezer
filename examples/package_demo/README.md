# package_demo

A demo package that can be loaded by tfloader.

Freeze command:

```bash
# cwd: root of tfreezer
python -m tfreezer.package_builder --pkgname my_package --workpath build/package_demo --distpath dist/package_demo --src-paths examples/package_demo/my_package
```
